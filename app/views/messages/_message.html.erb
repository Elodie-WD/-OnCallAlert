
<div id="message-<%= message.id %>">
        <small>
          <strong><%= message.user.first_name %></strong>
          <i><%= message.created_at.strftime("%d/%m/%Y %H:%M")%></i>
        </small>
        <p><%= message.content %></p>
      </div>
